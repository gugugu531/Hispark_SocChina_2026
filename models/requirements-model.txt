# 模型训练与 ONNX 导出环境依赖
# 依据来源（文档，非本机环境）：
#   海思 SS928 SDK《Yolov8 模型转换与部署》§2.1
# 环境：conda create -n <name> python=3.10   （文档要求 python>=3.8，示例用 3.10）
# 安装：pip install -r models/requirements-model.txt
#
# 说明：以下为 SDK 推荐的 PyTorch→ONNX 栈版本（与该 CANN 版本经官方文档验证兼容）。
torch==2.1.0
torchvision==0.16.0
numpy==1.26.4
opencv-python==4.8.1.78
onnx>=1.12.0
onnxsim>=0.4.1
PyYAML>=5.3.1
# 训练辅助
matplotlib>=3.2.2
#
# 注：若需复用 TensorFlow/Keras 权重（如 Zero-DCE Lite）转 ONNX，
#     需额外 tensorflow + tf2onnx（官方转换文档未覆盖，使用时自行锁定版本，opset=13）。
