# SocChina 板端运行配置。安装脚本只在文件不存在时创建，不覆盖板端已有设置。
#
# HDMI:
#   0 = 仅启动 RTSP，不初始化 VO/HDMI（默认）
#   1 = RTSP 与 HDMI 同时运行
ENABLE_HDMI=0

# RTSP/H.264
RTSP_PORT=8554
STREAM_PATH=live
BITRATE_KBPS=3000

# 相机与规则 Gamma 控制
SENSOR_INDEX=1
TONE_STRENGTH=0.7
